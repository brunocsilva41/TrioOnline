{
  "name": "db",
  "version": "1.0.0",
  "private": true,
  "main": "index.js",
  "scripts": {
    "build": "node scripts/with-env.cjs prisma generate",
    "db:generate": "node scripts/with-env.cjs prisma generate",
    "db:push": "node scripts/with-env.cjs prisma db push",
    "db:migrate": "node scripts/with-env.cjs prisma migrate dev",
    "db:migrate:deploy": "node scripts/with-env.cjs prisma migrate deploy"
  },
  "dependencies": {
    "@prisma/client": "5.22.0"
  },
  "devDependencies": {
    "prisma": "5.22.0"
  }
}
