services:
  # Game Server (Colyseus + Node.js)
  - type: web
    name: trinity-server
    env: node
    plan: free
    buildCommand: pnpm install && pnpm run build --filter game-server
    startCommand: pnpm run start --filter game-server
    healthCheckPath: /health
    envVars:
      - key: NODE_ENV
        value: production
      - key: DATABASE_URL # CORREÇÃO: Adicionada a chave para mapear o banco
        fromDatabase:
          name: trinity-db
          property: connectionString
      - key: REDIS_URL
        fromService:
          type: redis
          name: trinity-redis
          property: connectionString
      - key: PORT
        value: 10000

  # Game Client (Next.js PWA)
  - type: web
    name: trinity-client
    env: node
    plan: free
    buildCommand: pnpm install && pnpm run build --filter game-client
    startCommand: pnpm run start --filter game-client
    envVars:
      - key: NEXT_PUBLIC_GAME_SERVER_URL
        value: https://trinity-server.onrender.com
      - key: NODE_ENV
        value: production

  # Redis Service
  - type: redis
    name: trinity-redis
    plan: free
    ipAllowList: [] 

databases:
  - name: trinity-db
    plan: free
    databaseName: trinity
